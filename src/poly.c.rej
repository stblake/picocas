@@ -461,8 +461,10 @@
 }
 
+static int gcd_cnt = 0;
 static Expr* my_number_gcd(Expr* a, Expr* b) {
+    printf("GCD call %d\n", ++gcd_cnt); fflush(stdout);
     Expr* ca = get_int_content(a);
     Expr* cb = get_int_content(b);
 
 
 
